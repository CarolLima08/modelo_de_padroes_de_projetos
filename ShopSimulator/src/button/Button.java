package button;
//Declaração de interface Java que define uma interface pública denominada "Botão" e declara um único método abstrato chamado "paint", que não recebe argumentos e não retorna nenhum valor.
public interface Button {
	void paint();
}
